# 复制为 .env 后填入真实值，切勿提交 .env 到 Git
# cp .env.example .env   (Linux/macOS)
# copy .env.example .env (Windows)

# LLM / Embedding API（硅基流动等 OpenAI 兼容服务）
LLM_API_KEY=
EMBEDDING_API_KEY=

# MySQL（需与 docker/docker-compose.yaml 及 conf/app_config.yaml 保持一致）
MYSQL_USER=shopkeeper
MYSQL_PASSWORD=changeme
MYSQL_ROOT_PASSWORD=rootpass
